#include<stdio.h>
int main()
{
    int i,fact=1,number;
    printf("Enter a number:");
    scanf("%d",&number);
    for(i=1;i<=number;i++){
        fact=fact*i;
    }
    printf("Factorial of %d number is %d",number,fact);
    return 0;
}
output

Enter a number:5
Factorial of 5 number is 120
